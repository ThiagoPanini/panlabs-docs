---
# gerado por scripts/gerar-api.mjs — não editar à mão
title: "List customers"
description: "The account’s customers, most recent first, cursor-paginated."
sidebar_class_name: "api-metodo api-metodo--get"
api_exemplos: {"metodo":"GET","caminho":"/clientes","parametros":[{"nome":"limite","em":"query","exemplo":"20"},{"nome":"a_partir_de","em":"query","exemplo":""},{"nome":"email","em":"query","exemplo":""}],"exemplos":[{"linguagem":"bash","titulo":"cURL","modelo":"curl https://api.trilho.dev/v1/clientes?limite={{limite}}&a_partir_de={{a_partir_de}}&email={{email}} \\\n  -X GET \\\n  -H \"Authorization: Bearer $TRILHO_API_KEY\""},{"linguagem":"python","titulo":"Python","modelo":"trilho.clientes.listar({'limite': '{{limite}}', 'a_partir_de': '{{a_partir_de}}', 'email': '{{email}}'})"},{"linguagem":"javascript","titulo":"JavaScript","modelo":"await trilho.clientes.listar({limite: '{{limite}}', a_partir_de: '{{a_partir_de}}', email: '{{email}}'});"}],"respostas":[{"status":"200","titulo":"200","corpo":{"dados":[{"id":"cli_4Lp8qR","nome":"string","email":"string","documento":"52998224725","telefone":"+5511987654321","endereco":{"logradouro":"string","numero":"string","complemento":"string","bairro":"string","cidade":"string","estado":"SP","cep":"01310100"},"metadados":{},"criado_em":"2026-08-07T18:12:04Z"}],"proxima_pagina":"string"}},{"status":"401","titulo":"401","corpo":{"codigo":"chave_invalida","mensagem":"The key is missing, invalid, or from another environment."}},{"status":"429","titulo":"429","corpo":{"codigo":"limite_de_taxa_excedido","mensagem":"Rate limit exceeded. The header says how many calls remain."}}]}
---

# List customers

<VerbBadge verb="GET" /> `/clientes`

The account’s customers, most recent first, cursor-paginated.

## Parameters

<ParamField name="limite" type="integer" default="20">
How many items per page. Maximum 100.
</ParamField>

<ParamField name="a_partir_de" type="string">
The cursor of the last page read. Omit to get the first page.
</ParamField>

<ParamField name="email" type="string">
Filters by exact email.
</ParamField>

## Response

<ResponseField name="dados" type="array of object">
The customers on this page.

<Expandable title="each item of dados" defaultOpen>

<ResponseField name="id" type="string">
The customer identifier, prefixed `cli_`.
</ResponseField>

<ResponseField name="nome" type="string">
The full name.
</ResponseField>

<ResponseField name="email" type="string">
Used for receipts and declined-charge notifications.
</ResponseField>

<ResponseField name="documento" type="string">
CPF or CNPJ, digits only — no punctuation.
</ResponseField>

<ResponseField name="telefone" type="string">
In E.164, with country code.
</ResponseField>

<ResponseField name="endereco" type="object">
Required for boleto and for card address verification.

<Expandable title="endereco object" defaultOpen>

<ResponseField name="logradouro" type="string">
Street name.
</ResponseField>

<ResponseField name="numero" type="string">
The number, as a string — some addresses have none.
</ResponseField>

<ResponseField name="complemento" type="string">
Apartment, block.
</ResponseField>

<ResponseField name="bairro" type="string">
The neighborhood.
</ResponseField>

<ResponseField name="cidade" type="string">
The city.
</ResponseField>

<ResponseField name="estado" type="string">
The state, two letters.
</ResponseField>

<ResponseField name="cep" type="string">
Digits only.
</ResponseField>

</Expandable>
</ResponseField>

<ResponseField name="metadados" type="object">
Free-form, yours. Trilho neither reads nor validates the content.
</ResponseField>

<ResponseField name="criado_em" type="string">
When the customer was created.
</ResponseField>

</Expandable>
</ResponseField>

<ResponseField name="proxima_pagina" type="string">
The cursor for the next call. Null on the last page.
</ResponseField>

## Errors

| Status | When |
| --- | --- |
| `401` | The key is missing, invalid, or from another environment. |
| `429` | Rate limit exceeded. The header says how many calls remain. |

