---
# GERADO por scripts/generate-reference.mjs a partir de contracts/overpower.en.json.
# Não edite à mão: o portão 5 regenera e reprova em `git diff --exit-code`.
title: overpower list
description: Answers what is there. Bare, it prints the embedded catalog in four blocks; with a selector, it opens exactly one item, in full.
api_exemplos: {"signature":"overpower list [--skill <name>] [--bundle <name>] [--mcp <name>] [--ai-framework <name>] [--from <url>]","language":"bash","model":{"call":"overpower list","qualified":"overpower list","context":"always","parameters":[{"name":"--skill","type":"name","arity":{"multiple":false},"example":"panlabs-python-standards"},{"name":"--bundle","type":"name","arity":{"multiple":false}},{"name":"--mcp","type":"name","arity":{"multiple":false}},{"name":"--ai-framework","type":"name","arity":{"multiple":false}},{"name":"--from","type":"url","arity":{"multiple":false}}],"minimum":[{"context":"always","flags":[]}],"constraints":[{"type":"exclusive","precedence":1,"members":["--skill","--bundle","--mcp","--ai-framework"],"guard":null,"class":"TooManySelectorsError","exit":2,"literal":true,"message":"`list` shows one item at a time, and got {flags}"},{"type":"forbids","precedence":2,"when":"--from","forbidden":"--ai-framework","class":"UnsupportedRemoteListUnitError","exit":2,"literal":true,"message":"`--from` on `list` shows skills, MCP servers and bundles — an AI Framework does not exist remotely: it is a folder of the overpower's own wheel, and --ai-framework names one of those"}]}}
---

# overpower list

{/* cita-saida-de-ferramenta */}

**Command** · `overpower list`

<CommandPanel />

Every entry prints with its size, its file count and its description in full, never truncated, because the description is the thing the decision to install is actually made on. Under it sits the line that installs the item, with no prompt in front, so that copying and pasting hands back something that runs.

`list` answers about one item at a time. Two selectors on the same line are a question with two answers, and the command refuses rather than silently picking one.

## Options

<ParamField name="--skill" type="name">
Opens one pool skill in full. Takes the short form `-s`.
</ParamField>

<ParamField name="--bundle" type="name">
Opens a bundle and lists the artifacts its manifest names. Takes the short form `-b`.
</ParamField>

<ParamField name="--mcp" type="name">
Opens an MCP server's recipe, with the fields the declaration itself spells and the ones overpower derives from it, the transport among those when the recipe carries `source:`. A recipe is an entry of the YAML manifest's `mcp:` key, not a file beside it.
</ParamField>

<ParamField name="--ai-framework" type="name">
Opens an AI Framework and prints every artifact inside it. It is the one selector `--from` refuses.
</ParamField>

<ParamField name="--from" type="url">
Reads from a GitHub repository instead of the embedded catalog. Bare, it prints that repository's showcase.
</ParamField>
