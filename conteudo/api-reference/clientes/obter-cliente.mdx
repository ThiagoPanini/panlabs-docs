---
# gerado por scripts/gerar-api.mjs — não editar à mão
title: "Obter cliente"
description: "Um cliente pelo `id`."
sidebar_class_name: "api-metodo api-metodo--get"
api_exemplos: {"metodo":"GET","caminho":"/clientes/{id}","parametros":[{"nome":"id","em":"path","exemplo":"cli_9Xz4mT"}],"exemplos":[{"linguagem":"bash","titulo":"cURL","modelo":"curl https://api.trilho.dev/v1/clientes/{{id}} \\\n  -X GET \\\n  -H \"Authorization: Bearer $TRILHO_API_KEY\""},{"linguagem":"python","titulo":"Python","modelo":"trilho.clientes.obter('{{id}}')"},{"linguagem":"javascript","titulo":"JavaScript","modelo":"await trilho.clientes.obter('{{id}}');"}],"respostas":[{"status":"200","titulo":"200","corpo":{"id":"cli_4Lp8qR","nome":"string","email":"string","documento":"52998224725","telefone":"+5511987654321","endereco":{"logradouro":"string","numero":"string","complemento":"string","bairro":"string","cidade":"string","estado":"SP","cep":"01310100"},"metadados":{},"criado_em":"2026-08-07T18:12:04Z"}},{"status":"401","titulo":"401","corpo":{"codigo":"chave_invalida","mensagem":"A chave está ausente, é inválida, ou é de outro ambiente."}},{"status":"404","titulo":"404","corpo":{"codigo":"recurso_nao_encontrado","mensagem":"O recurso não existe nesta conta."}}]}
---

# Obter cliente

<VerbBadge verb="GET" /> `/clientes/{id}`

Um cliente pelo `id`.

## Parâmetros

<ParamField name="id" type="string" required>
O `id` do cliente.
</ParamField>

## Resposta

<ResponseField name="id" type="string">
O identificador do cliente, com prefixo `cli_`.
</ResponseField>

<ResponseField name="nome" type="string">
O nome completo.
</ResponseField>

<ResponseField name="email" type="string">
Usado para recibo e para notificação de cobrança recusada.
</ResponseField>

<ResponseField name="documento" type="string">
CPF ou CNPJ, só dígitos — sem pontuação.
</ResponseField>

<ResponseField name="telefone" type="string">
Em E.164, com código do país.
</ResponseField>

<ResponseField name="endereco" type="object">
Necessário para boleto e para verificação de endereço no cartão.

<Expandable title="objeto endereco" defaultOpen>

<ResponseField name="logradouro" type="string">
Rua ou avenida.
</ResponseField>

<ResponseField name="numero" type="string">
O número, como string — endereços têm "s/n".
</ResponseField>

<ResponseField name="complemento" type="string">
Apartamento, bloco.
</ResponseField>

<ResponseField name="bairro" type="string">
O bairro.
</ResponseField>

<ResponseField name="cidade" type="string">
A cidade.
</ResponseField>

<ResponseField name="estado" type="string">
A UF, duas letras.
</ResponseField>

<ResponseField name="cep" type="string">
Só dígitos.
</ResponseField>

</Expandable>
</ResponseField>

<ResponseField name="metadados" type="object">
Livre, seu. O Trilho não lê nem valida o conteúdo.
</ResponseField>

<ResponseField name="criado_em" type="string">
Quando o cliente foi criado.
</ResponseField>

## Erros

| Status | Quando |
| --- | --- |
| `401` | A chave está ausente, é inválida, ou é de outro ambiente. |
| `404` | O cliente não existe nesta conta. |

