---
# gerado por scripts/gerar-api.mjs — não editar à mão
title: "Listar assinaturas"
description: "As assinaturas da conta, mais recentes primeiro, paginadas por cursor."
sidebar_class_name: "api-metodo api-metodo--get"
api_exemplos: {"metodo":"GET","caminho":"/assinaturas","parametros":[{"nome":"limite","em":"query","exemplo":"20"},{"nome":"a_partir_de","em":"query","exemplo":""},{"nome":"status","em":"query","exemplo":""}],"exemplos":[{"linguagem":"bash","titulo":"cURL","modelo":"curl https://api.trilho.dev/v1/assinaturas?limite={{limite}}&a_partir_de={{a_partir_de}}&status={{status}} \\\n  -X GET \\\n  -H \"Authorization: Bearer $TRILHO_API_KEY\""},{"linguagem":"python","titulo":"Python","modelo":"trilho.assinaturas.listar({'limite': '{{limite}}', 'a_partir_de': '{{a_partir_de}}', 'status': '{{status}}'})"},{"linguagem":"javascript","titulo":"JavaScript","modelo":"await trilho.assinaturas.listar({limite: '{{limite}}', a_partir_de: '{{a_partir_de}}', status: '{{status}}'});"}],"respostas":[{"status":"200","titulo":"200","corpo":{"dados":[{"id":"sub_7Bn3wK","cliente_id":"string","status":"ativa","meio":"pix","plano":{"nome":"string","valor":0,"intervalo":"mensal"},"proxima_cobranca_em":"2026-08-07T18:12:04Z","cancelada_em":"2026-08-07T18:12:04Z","criado_em":"2026-08-07T18:12:04Z"}],"proxima_pagina":"string"}},{"status":"401","titulo":"401","corpo":{"codigo":"chave_invalida","mensagem":"A chave está ausente, é inválida, ou é de outro ambiente."}},{"status":"429","titulo":"429","corpo":{"codigo":"limite_de_taxa_excedido","mensagem":"Limite de taxa excedido. O cabeçalho diz quantas chamadas sobram."}}]}
---

# Listar assinaturas

<VerbBadge verb="GET" /> `/assinaturas`

As assinaturas da conta, mais recentes primeiro, paginadas por cursor.

## Parâmetros

<ParamField name="limite" type="integer" default="20">
Quantos itens por página. Máximo 100.
</ParamField>

<ParamField name="a_partir_de" type="string">
O cursor da última página lida. Omitir traz a primeira página.
</ParamField>

<ParamField name="status" type="string">
Filtra por um único status.
</ParamField>

## Resposta

<ResponseField name="dados" type="array de object">
As assinaturas desta página.

<Expandable title="cada item de dados" defaultOpen>

<ResponseField name="id" type="string">
O identificador da assinatura, com prefixo `sub_`.
</ResponseField>

<ResponseField name="cliente_id" type="string">
O `id` do cliente cobrado.
</ResponseField>

<ResponseField name="status" type="string">
`inadimplente` é o estado depois de todas as retentativas de uma cobrança falharem, sem cancelamento automático.

Valores possíveis: `ativa`, `pausada`, `cancelada`, `inadimplente`.
</ResponseField>

<ResponseField name="meio" type="string">
Boleto não recorre sozinho — não está nesta lista.

Valores possíveis: `pix`, `cartao`.
</ResponseField>

<ResponseField name="plano" type="object">
O valor e a cadência.

<Expandable title="objeto plano" defaultOpen>

<ResponseField name="nome" type="string">
O nome do plano, seu — o Trilho não valida contra um catálogo.
</ResponseField>

<ResponseField name="valor" type="integer">
Em centavos, cobrado a cada ciclo.
</ResponseField>

<ResponseField name="intervalo" type="string">
A cadência da cobrança.

Valores possíveis: `mensal`, `anual`.
</ResponseField>

</Expandable>
</ResponseField>

<ResponseField name="proxima_cobranca_em" type="string">
Nula quando `status` é `cancelada`.
</ResponseField>

<ResponseField name="cancelada_em" type="string">
Quando a assinatura foi cancelada.
</ResponseField>

<ResponseField name="criado_em" type="string">
Quando a assinatura foi criada.
</ResponseField>

</Expandable>
</ResponseField>

<ResponseField name="proxima_pagina" type="string">
O cursor da próxima chamada. Nulo na última página.
</ResponseField>

## Erros

| Status | Quando |
| --- | --- |
| `401` | A chave está ausente, é inválida, ou é de outro ambiente. |
| `429` | Limite de taxa excedido. O cabeçalho diz quantas chamadas sobram. |

